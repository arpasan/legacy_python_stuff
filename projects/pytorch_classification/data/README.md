# flower_photos_mini

Tiny curated subset (~5 train / ~3 test images per class) for a fast, reproducible CNN demo.

To use a larger local set, point `DATA_ROOT` in the notebook at a full `flower_photos` tree with the same `training_set/training.csv` + `test_set/testing.csv` schema.
