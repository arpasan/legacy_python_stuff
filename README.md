# Python Stuff

Legacy Python / ML examples, modernized so they still run on current package versions.

Each analysis lives in its own topic folder with co-located `data/` (no nested `projects/` wrapper).

* [Image `PyTorch` Classification without `torchvision`: `torch`, `pandas`, `skimage`, `numpy`, `sklearn`](https://github.com/arpasan/legacy_python_stuff/blob/main/pytorch_classification/pytorch_classification_without_torchvision.ipynb)
