# flower_photos_mini

Curated subset of the flower-photos extract: **80 train / 20 test images per class**
(daisy, dandelion, roses, sunflowers, tulips). A few non-flower-dominant source frames
are omitted.

Layout:

- `training_set/training.csv` + images
- `test_set/testing.csv` + images
